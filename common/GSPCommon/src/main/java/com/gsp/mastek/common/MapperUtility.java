package com.gsp.mastek.common;

public class MapperUtility {
	
	
	/*public static void processErrorDetails(GenericResponse responseReturn,
			Response<Result> response) {
		responseReturn.setResponseType(ResponseType.ERROR);
		com.squareup.okhttp.Response result = response.raw();
		
		Error error = new Error();
		responseReturn.setError(error);			
		String code = ""+result.code();
		String resString = UPIResourceBundler.getStringResource(code);
		
		error.setCode(code + resString);
		error.setErrorDetails(result.message());

	}*/

}
